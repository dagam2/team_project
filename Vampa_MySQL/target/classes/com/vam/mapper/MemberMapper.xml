<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.vam.mapper.MemberMapper">
  
	<!-- 회원가입 -->  
  	<insert id="memberJoin">
  	
  		insert into book_member values(#{memberId}, #{memberPw}, #{memberName}, #{memberSex}, #{memberPhoneNum}, #{memberBirth}, #{memberMail}, #{memberAddr1}, #{memberAddr2}, #{memberAddr3}, 0)

  	</insert>
  	  	<!-- 아이디 중복검사 -->
  	<select id="idCheck" resultType="int">
  	
  		select count(*) from book_member where memberId = #{memberId}
  	
  	</select>
      <!-- 로그인 -->
      <select id="memberLogin" resultType="com.vam.model.MemberVO">
      select memberId, memberName, adminck from book_member where memberId = #{memberId} and memberPw = #{memberPw}
      
      </select>
	
		<!-- 비밀번호 변경 -->
		<update id="updatepw" >
			update book_member set memberPw = #{memberPw} where memberId = #{memberId}
		</update>
		
		<select id="selectMemberID" resultType="String">
			select memberId from book_member 
			where memberId=#{memberId} and memberMail=#{email}
		</select>
  	
  </mapper>